upstream py_backend {
    server django-backend:8000;
}

server {

    listen 8080;

    location / {
        proxy_pass http://py_backend;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
 	location /static/ {
        alias /home/py_backend/web/staticfiles/;
    }
	location /media/ {
		alias /home/py_backend/web/mediafiles/;
	}
}